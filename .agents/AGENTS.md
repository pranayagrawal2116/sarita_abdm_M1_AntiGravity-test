# Workspace Agent Rules

## Pre-Flight Build Check
Before making any code changes, verify that the project builds successfully and that the previous prompt's implementation has no compilation errors. If there are existing build or compilation issues introduced by earlier prompts, fix those first before implementing the current prompt. Do not continue building new functionality on top of a broken codebase.
