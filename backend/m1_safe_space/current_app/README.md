Frozen backend safe space mirror.

This folder contains the current backend source copied as `.txt` reference files.

These files are for protection and reference only and are not used by runtime.
This mirror is part of the larger `lib/m1_safe_space/current_app/` full-M1 safe snapshot.
