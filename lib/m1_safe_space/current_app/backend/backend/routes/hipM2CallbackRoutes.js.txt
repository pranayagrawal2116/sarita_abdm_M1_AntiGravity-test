const express = require("express");
const controller = require("../controllers/hipLinkingController");

const router = express.Router();

router.post("/token/on-generate-token", controller.onGenerateToken);

module.exports = router;
